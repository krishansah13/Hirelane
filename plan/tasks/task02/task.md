# Task 02 - Auth.js credentials, JWT role, login page

**Status:** done (already in repo)

## Scope

Auth.js Credentials provider, JWT session with `id` and `role`, `/login` with demo accounts.

## Done when

- Seekers and employers can sign in
- Session exposes `user.id` and `user.role`
- Unauthenticated users are sent to `/login`
