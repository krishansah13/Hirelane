# Task 03 - Route groups, proxy, loading/error/404

**Status:** done (already in repo)

## Scope

`(public)` vs `(dashboard)` route groups. `src/proxy.ts` matches `/dashboard` and `/employer`. Root `loading.tsx`, `error.tsx`, `not-found.tsx`.

## Done when

- Dashboard routes require a session
- Role layouts redirect seeker ↔ employer
- Unknown routes 404
