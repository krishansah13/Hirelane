# Task 01 - Schema, seed, MongoDB connection

**Status:** done (already in repo)

## Scope

Four Mongoose models (`User`, `Company`, `Job`, `Application`), cached Mongo connection, seed of ~50 jobs across several companies.

## Done when

- Models match the SRS field set
- `npm run seed` creates companies, employers, seekers, jobs, and applications
- Demo accounts exist for seeker and employer
